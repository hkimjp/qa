services:
  qa:
    image: eclipse-temurin:25-jre
    environment:
      - PORT
      - DATABASE_URL
    working_dir: /usr/src/app
    volumes:
      - .:/usr/src/app
      - m2:/root/.m2
    ports:
      - 8530:8530
    entrypoint: >
      sh -c "java --enable-native-access=ALL-UNNAMED -jar qa.jar"

  db:
    image: postgres:18
    environment:
      - POSTGRES_PASSWORD
    volumes:
      - data:/var/lib/postgresql
      - ./dumps:/dumps

volumes:
  m2:
    external: true
  data:
    driver: local
